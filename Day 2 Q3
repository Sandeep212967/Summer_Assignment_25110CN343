#include<iostream>
using namespace std;
int main()  
{
    int n,product=1,d;
    cout<<"Enter the number: "; 
    cin>>n;
    while(n>0)
    {
        d = n % 10;
        product = product*d;
        n = n / 10;  
    }
    cout<<"Product of digits: "<<product;
    
    return 0;
}
