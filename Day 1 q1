#include <iostream>
using namespace std;
int main()
{
    int n, sum = 0;

    cout << "Enter the n natural number:";
    cin >> n;

    sum = (n * (n + 1)) / 2;

    cout << "Sum = " << sum ;

    return 0;
}
