#include<iostream>
using namespace std;
int main()  
{
    int n,reverse=0,d;
    cout<<"Enter the number: "; 
    cin>>n;
    while(n>0)
    {
        d = n % 10;
        reverse = reverse * 10 + d;
        n = n / 10;  
    }
    cout<<"Reverse of the number: "<<reverse;
    
    return 0;
}
